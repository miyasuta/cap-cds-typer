_schema-version: "3.1"
ID: capts
description: A simple CAP project.
version: 1.0.0
modules:
- name: capts-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: capts-auth
  - name: capts-db
  - name: capts-appllog
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: capts
  type: approuter.nodejs
  path: app/
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: capts-auth
  - name: capts-appllog
  - name: capts-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: capts-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: capts-db
  parameters:
    buildpack: nodejs_buildpack
- name: capts-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: capts-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - miyasutaordersui.zip
      name: miyasutaordersui
      target-path: resources/
- name: miyasutaordersui
  type: html5
  path: app/ordersui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: capts-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: capts-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: capts-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: capts-appllog
  type: org.cloudfoundry.managed-service
  parameters:
    service: application-logs
    service-plan: lite
- name: capts-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: capts-html5-srv
    service-plan: app-host
- name: capts-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: capts-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
    - npm run build:cf
